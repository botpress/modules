[X] Database schema
  [X] schedules:
    id(name),
    enabled(bool), 
    schedule_type(string, [cron, exp, once]), 
    schedule(string, [date for once, strings for exp and cron]), 
    schedule_human(string, human-friendly),
    created_on(date), 
    action(string)
  [X] schedules_tasks: scheduleId, scheduleOn, status, logs, finishedOn, returned

[X] Scheduler: compute next occurence

[X] Loop: run expired (every 5s)
  [X] Run: reschedule next occurence
  [X] Update task status
  [X] Execute action if ENABLED
  [X] Update task status + returned + logs

[X] API
  [X] Create new schedule
    [X] Validate type and expression
  [X] Modify schedule
  [X] Delete schedule
    [X] Delete scheduled tasks
  [X] Get upcoming tasks
  [X] Get previous/past tasks

[X] Events API
  [X] On task started
  [X] On task finished
